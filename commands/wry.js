const Command = require("../util/command").Command;

exports.command = new Command(
    "wry",
    "Re!",
    "wry",
    (self, client, message, args) => {
        message.channel.send("https://waa.ai/eA7f");
    }
);
